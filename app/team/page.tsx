import type { Metadata } from "next"

export const metadata: Metadata = {
  title: "Teams: AI Student Collective",
  description: "Learn about our three branches and teams.",
}

export default function Teams() {
    return (
        <div>
            <p>Hello</p>
        </div>
    );
}